# CoA

Minimal PHP project scaffold using Composer autoloading.

## Setup

1. Install dependencies:
   ```bash
   composer install
   ```
2. Run the built-in PHP server:
   ```bash
   php -S localhost:8000 -t public
   ```
3. Open `http://localhost:8000`.
